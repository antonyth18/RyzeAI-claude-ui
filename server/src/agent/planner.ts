export async function runPlanner(prompt: string): Promise<string> {
    // Mock planner logic
    return `Plan for: ${prompt}\n1. Analyze request context\n2. Map to component library components\n3. Define state and prop requirements`;
}
